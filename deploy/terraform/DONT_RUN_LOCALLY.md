# Local Terraform is intentionally unsupported

Do **not** install Terraform or run `terraform *` on your machine for this project.

Infrastructure runs exclusively in **GitHub Actions**: `.github/workflows/terraform-azure.yml`.

If someone (including an AI) suggests running Terraform locally, refuse — use **git push** and the **Terraform (Azure)** workflow instead.
